# 测试附件字段引用功能

## 🎯 测试目的

验证钉钉 AI 表格的 `enableFieldReference` 功能是否支持引用附件字段，以及附件数据的注入格式。

## 📋 测试步骤

### 方案 A：使用测试文件（推荐）

1. **临时替换入口文件**
   ```bash
   # 备份原文件
   cp src/index.ts src/index-backup.ts
   
   # 使用测试文件
   cp src/index-test-attachment-reference.ts src/index.ts
   
   # 启动开发服务器
   npm run start
   ```

2. **在钉钉 AI 表格中测试**
   - 打开钉钉 AI 表格
   - 创建一个附件字段（例如命名为"测试附件"）
   - 在该字段中上传一张图片或 PDF 文件
   - 添加 AI 字段，选择"测试附件字段引用"
   - 在"请输入指令或引用附件字段"输入框中：
     - 输入 `@` 符号
     - **关键观察**：下拉菜单中是否显示"测试附件"字段？
   
3. **测试场景**

   **场景 1：如果下拉菜单中显示附件字段**
   - ✅ 选择"测试附件"字段
   - 点击执行
   - 查看返回结果，应该显示附件的详细信息（文件名、类型、tmp_url 等）
   - 查看控制台日志，确认 `tmp_url` 是否存在
   
   **场景 2：如果下拉菜单中不显示附件字段**
   - ❌ 说明当前环境不支持附件字段引用
   - 需要继续使用 FieldSelect 方案

4. **恢复原文件**
   ```bash
   # 测试完成后恢复
   cp src/index-backup.ts src/index.ts
   ```

### 方案 B：直接在现有代码中测试

如果你不想替换文件，可以：

1. 在钉钉 AI 表格中创建一个附件字段
2. 在现有的"自定义 AI 任务指令"输入框中输入 `@`
3. 观察下拉菜单中是否显示附件字段

## 🔍 预期结果

### ✅ 如果支持附件字段引用

**UI 表现：**
- 输入 `@` 后，下拉菜单中会显示附件字段
- 选择后，输入框中会显示类似 `@测试附件` 的标记

**数据格式：**
```json
{
  "name": "test.png",
  "type": "image/png",
  "size": 123456,
  "tmp_url": "https://alidocs.dingtalk.com/xxx/test.png?Expires=xxx&OSSAccessKeyId=xxx&Signature=xxx"
}
```

或多个附件时：
```json
[
  {
    "name": "test1.png",
    "type": "image/png",
    "size": 123456,
    "tmp_url": "https://..."
  },
  {
    "name": "test2.pdf",
    "type": "application/pdf",
    "size": 789012,
    "tmp_url": "https://..."
  }
]
```

**测试文件返回结果：**
```
✅ 成功解析为附件数组！
共 1 个附件

附件 1:
  - 文件名: test.png
  - 类型: image/png
  - 大小: 123456 bytes
  - tmp_url: ✅ 存在
  - URL 长度: 256 字符
```

### ❌ 如果不支持附件字段引用

**UI 表现：**
- 输入 `@` 后，下拉菜单中只显示文本类字段（如标题、文本等）
- 不显示附件字段

**测试文件返回结果：**
```
❌ 不是附件引用（无法解析为 JSON）

接收到的原始文本:
用户输入的文本内容

这可能是：
1. 普通文本输入
2. 引用了文本字段（如标题）
3. 附件字段引用功能不可用
```

## 📊 测试结论

根据测试结果，我们可以得出以下结论：

### 情况 1：支持附件字段引用 ✅
- 可以移除 `refImage` FieldSelect 组件
- 直接在 `aiInstruction` 中通过 `@` 引用附件字段
- 在 `execute` 函数中通过 `JSON.parse(aiInstruction)` 获取附件信息
- 提取 `tmp_url` 发送给 Python 服务

### 情况 2：不支持附件字段引用 ❌
- 继续使用当前的 FieldSelect 方案
- 用户通过下拉框选择附件字段
- 这是目前唯一可行的方案

## 🎯 下一步行动

根据测试结果选择：

**如果支持附件字段引用：**
1. 修改代码，移除 `refImage` FieldSelect
2. 在 `execute` 中添加 JSON 解析逻辑
3. 支持混合模式：既可以引用附件，也可以输入普通文本

**如果不支持附件字段引用：**
1. 保持当前的 FieldSelect 方案
2. 继续使用 `refImage` 多选框
3. 这已经是最优方案

## 📝 注意事项

1. **环境差异**：不同钉钉版本可能支持程度不同
2. **权限问题**：某些功能可能需要特定权限或配置
3. **文档滞后**：官方文档可能不是最新的

## 🔗 相关资源

- 官方文档：`/Users/tian/field-decorator-demo/钉钉ai表格字段开发指南（Faas）.md`
- 当前实现：`/Users/tian/field-decorator-demo/src/index.ts`
- 测试文件：`/Users/tian/field-decorator-demo/src/index-test-attachment-reference.ts`
